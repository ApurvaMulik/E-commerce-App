import 'package:flutter/material.dart';

class myaccpage extends StatelessWidget {
  const myaccpage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        child : Center(
          child: Text('My account'),
        ));
  }
}
